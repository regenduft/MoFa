#!/usr/bin/perl
use warnings;
use strict;

use lib('../');
use Mofa::Debuging;
use Mofa::Controller;
use Mofa::Model;
use CGI qw(:standard);
use CGI::Carp qw(fatalsToBrowser);
print "content-type: text/plain\n\n";

#my $description="Stelzenberg";
#my @aa = geocode($description);
#my $key; my $value;
#while (($key,$value) = each(%{$a[0]})) {
#print $key, "  ", $value, "\n";
#}
#for (@aa) {
# print ${$_}{accuracy}, "\n";
#}
#print (join("\n", Mofa::Controller::create_tables()));
print "\n\n\n";
print( join( "\n", Mofa::Controller::cp() ) );

